/target
**/*.rs.bk
Cargo.lock

# Agent memory files - written by the agent at runtime, not committed
memory/
**/memory/
.agent-memory/

# Local config with API keys — use .params.example as the template
.params
